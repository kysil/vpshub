# WireGuard

This directory contains WireGuard documentation, configuration templates, and reusable components for `vpshub`.

The WireGuard architecture should support secure and reproducible VPN deployments while keeping private keys, peer-specific configurations, addresses, and other sensitive deployment data outside version control.

Reusable configuration templates should be stored in the `templates/` directory.
