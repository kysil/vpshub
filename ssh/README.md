# SSH

This directory contains SSH hardening documentation, configuration examples, and reusable components for `vpshub`.

The SSH security model should follow modern authentication and access-control practices, including key-based authentication and the principle of least privilege.

Configuration examples must remain portable and must not contain deployment-specific addresses, credentials, private keys, or other sensitive information.
