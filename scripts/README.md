# Scripts

This directory contains reusable scripts for deploying, configuring, maintaining, and validating `vpshub` infrastructure.

Scripts should be designed to be readable, auditable, and safe to execute.

Where practical, scripts should be idempotent and should validate required conditions before making system changes.

Deployment-specific credentials, secrets, addresses, and other sensitive data must not be embedded in scripts or committed to the repository.
