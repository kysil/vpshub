# Firewall

This directory contains firewall documentation, configuration examples, and reusable components for `vpshub`.

The project follows a default-deny inbound security model with only explicitly required network traffic permitted.

Provider-level and host-level firewall controls should be documented separately to preserve a clear layered security architecture.

Deployment-specific addresses, credentials, and other sensitive information must not be committed to the repository.
